(module jazz.designer.surrogate.Surrogate-Handler jazz
  (import (jazz.handler))
  (class Surrogate-Handler extends Event-Handler
    (slot preserved getter generate)
    (method protected override (initialize . rest))))
